[pytest]
testpaths = tests
filterwarnings =
    # Flask-Migrate 4.1 still calls SQLAlchemy's deprecated get_engine() in
    # migrations/env.py; nothing we can fix from here.
    ignore:'get_engine' is deprecated:DeprecationWarning
